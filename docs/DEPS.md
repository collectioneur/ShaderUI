# Dependencies

GPU and shader code in this project is built on **TypeGPU**.

- **Documentation**: https://docs.swmansion.com/TypeGPU/
- **Versions**: See root `package.json`. TypeGPU and `@typegpu/*` packages are pinned to the same major/minor (e.g. `0.9.0`) for compatibility.
